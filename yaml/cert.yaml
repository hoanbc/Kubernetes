apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nginxdev-tls
spec:
  secretName: nginxdev-tls
  issuerRef:
    name: acme-issuer
    kind: ClusterIssuer
  commonName: nginxdev.testvn.click
  dnsNames:
  - nginxdev.testvn.click
